# ── Python ────────────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ── Sistema ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Variáveis de ambiente ─────────────────────────────────────────────────────
.env
.env.*
.env.local
.env.*.local
*.env
# Exemplos sem secrets — devem ser commitados
!.env.example
!.env.production.example
!.env.docker.example
!.env.superadmin.example

# ── Dados de clientes / exports (PII — LGPD; NUNCA versionar) ──────────────────
# Exports brutos da Trinks e afins ficam locais, fora do git. Versione só o
# CÓDIGO do importador e fixtures ANONIMIZADAS.
TrinksInformations/
/data/
*_export.csv
*_export.xlsx

# ── Credenciais e secrets ─────────────────────────────────────────────────────
credentials.json
*credentials*.json
*credential*.json
*secret*.json
*secret*.yaml
*secret*.yml
service-account*.json
google-credentials*.json
firebase-adminsdk*.json
*.key
*.pem
*.p12
*.pfx

# ── Backups e arquivos temporários ────────────────────────────────────────────
backups/
backup-*.json
backup-*.tar.gz
*-backup.json
*-backup-*.json
workflows-backup*.json
*.tar.gz
*.bak

# ── n8n — exports de workflow (podem conter API keys e webhooks) ──────────────
# workflows.json é versionado de propósito (scripts patcham ele; credenciais já
# usam $env.BOT_API_KEY). Apenas exports ad-hoc e backups ficam fora do git.
workflows-*.json
n8n-export*.json
